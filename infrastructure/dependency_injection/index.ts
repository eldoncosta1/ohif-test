import {
  container,
  inject,
  injectable,
  instancePerContainerCachingFactory,
  Lifecycle,
  scoped,
} from 'tsyringe';
import './providers/repositories';

export { container, inject, injectable, instancePerContainerCachingFactory, Lifecycle, scoped };
